apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
data:
  HOST: "0.0.0.0"
  CALENDAR_SOURCE: "google"
  DIRECTUS_URL: "http://directus:8055"
  PUBLIC_TURNSTILE_SITE_KEY: "0x4AAAAAADoWbB9LfqX6jMbE"
  # To use Google Calendar instead of Directus as the event source, set:
  #   CALENDAR_SOURCE: "google"
  GOOGLE_CALENDAR_ID: "7440d5a724f188483234a2373a7a5c1ea759a57a881904200fdca1c321e8784b@group.calendar.google.com"
  DEFAULT_EVENT_CAPACITY: "40"   # optional, source-agnostic fallback when an event has no capacity
  # and provide GOOGLE_CALENDAR_API_KEY via the directus-secret (key: google-api-key).
